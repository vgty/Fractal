- @posts.each do |post|
  = link_to (image_tag post.image.url(:small)), post_path(post)
  %h2= link_to post.title, post_path(post)
  %p= pluralize(post.comments.count, "Comment")
%h2= link_to "Add inspiration", new_post_path
